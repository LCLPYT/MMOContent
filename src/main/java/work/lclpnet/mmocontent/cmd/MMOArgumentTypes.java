package work.lclpnet.mmocontent.cmd;

import com.mojang.brigadier.arguments.ArgumentType;
import net.minecraft.command.argument.ArgumentTypes;
import net.minecraft.command.argument.serialize.ArgumentSerializer;
import net.minecraft.util.Identifier;

public class MMOArgumentTypes {

//    public static <T extends ArgumentType<?>> void register(Identifier id, Class<T> argClass, ArgumentSerializer<T> serializer) {
//        ArgumentTypes.register(id.toString(), argClass, serializer);
//    }
}
